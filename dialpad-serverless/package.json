{
  "name": "dialpad-serverless",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "fetch": "^1.1.0",
    "node-fetch": "^2.6.6",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.4.0"
  },
  "engines": {
    "node": "10"
  }
}
